
<div class="container">
  <div class="row">
     <div class="mx-5 col-sm-6 col-md-6 col-lg-5 px-1 px-sm-1">

   <h5><p class = "bg-light mt-3 col-4 md-3">顧客一覧画面</p></h5>
    </div>
     </div>
     </div>
<center>

       <table class="table col-md-5">
      <thead class="thead-light">
        <tr>
           <th>会員ID</th>
           <th>氏名</th>
           <th>メールアドレス</th>
           <th>ステータス</th>
        </tr>
      </thead>
   <tbody>

     <% @customers.each do |customer| %>
      <tr>
            <td><%= customer.id %></td>
            <td><%= link_to admin_customers_show_path(customer.id) do %>
             <%= customer.full_name %>
            <% end %></td>
            <td><%= customer.email %></td>
            <td><%= customer.is_deleted_i18n %></td>
      </tr>
     <% end %>
  <%#= link_to "back", admin_homes_top_path %>
  </tbody>
</table>

            <%#= link_to "show", admin_customers_show_path(customer.id) %>
          <%#= link_to admin_customers_edit_path(customer.id) do %>
            <%#= customer.last_name %><%#= customer.first_name %>
